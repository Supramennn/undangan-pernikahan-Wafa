const { chromium } = require('playwright'); const fs = require('fs'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); console.log('Navigating...'); await page.goto('https://ourmoment.link/mita-afif/?to=abdul', { waitUntil: 'networkidle' }); try { await page.click('button:has-text(Buka)'); await page.waitForTimeout(2000); } catch (e) {} for (let i = 0; i < 5; i++) { await page.evaluate(() => window.scrollBy(0, window.innerHeight)); await page.waitForTimeout(1000); } await page.screenshot({ path: 'ref_screenshot_full.png', fullPage: true }); const html = await page.content(); fs.writeFileSync('ref_page.html', html); console.log('Done.'); await browser.close(); })();
